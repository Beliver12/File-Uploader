<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title></title>
</head>

<body>
    <h1>File Uploader app</h1>
    <h2>
        <%= folder.folderName %>
    </h2>
    <h2>
        <%= user.username %>
    </h2>
    <h3>
        <a href="/log-out">LOG OUT</a>
    </h3>
    <form action="/folders/<%= folder.id %>" method="POST" enctype="multipart/form-data">
        <label for="myfile">Upload a file:</label>
        <input type="file" id="myfile" name="myfile"><br><br>
        <input type="submit">
    </form>
    <% if (locals.files) {%>
        <ul>
            <% for (let i=0; i < files.length; i++) { %>
                <li>
                    <a href="/files/<%= files[i].id %>">
                        <%= files[i].fileName %>
                    </a>


                    <a href="/deleteFile/<%= files[i].id %>">
                        <button>Delete</button>
                    </a>
                </li>
                <%}%>
        </ul>
        <%}%>
</body>

</html>