<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title></title>
</head>

<body>

    <% if (locals.user) {%>
        <div>
            <h1>File Uploader app</h1>
            <h2>
                <%= user.username %>
            </h2>
            <h3>
                <a href="/log-out">LOG OUT</a>
            </h3>
        </div>
        <form action="" method="POST">
            <label for="myfolder">Create folder:</label>
            <input type="text" id="myfolder" name="myfolder">
            <input type="submit">
        </form>

        <% if (locals.folder) {%>
            <ul>

                <% for (let i=0; i < folder.length; i++) { %>

                    <li>
                        <a href="/folders/<%= folder[i].id %>">

                            <%= folder[i].folderName %>

                        </a>

                        <a href="/update/<%= folder[i].id %>">
                            <button>Update</button>
                        </a>
                        <form id="delete" action="/delete/<%= folder[i].id %> " method="POST" style="display:inline;">
                            <button type="submit"
                                onclick="return confirm('Are you sure you want to delete this folder? you will lose all files inside that folder.' )">Delete</button>
                        </form>

                    </li>
                    <%}%>

            </ul>
            <% }%>

                <%} else { %>

                    <h1>please log in</h1>
                    <%- include("errors.ejs") %>
                        <% if (locals.messages) {%>
                            <p>
                                <%= messages[0] %>
                            </p>
                            <%}%>
                                <form action="/log-in" method="POST">
                                    <label for="username">Username</label>
                                    <input id="username" name="username" placeholder="username" type="text" />
                                    <label for="password">Password</label>
                                    <input id="password" name="password" type="password" />
                                    <button>Log In</button>
                                </form>
                                <a href="/sign-up">Sign-up</a>


                                <% }%>
</body>

</html>